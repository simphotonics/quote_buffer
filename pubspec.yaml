name: quote_buffer

description: Extension on StringBuffer that simplifies transforming single
  objects and lists of objects into quoted strings.

version: 0.2.3

homepage: https://github.com/simphotonics/quote_buffer

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies: null

dev_dependencies:
  ansicolor: ^2.0.1
  pedantic: ^1.11.0
  test: ^1.16.8
