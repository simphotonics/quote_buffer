name: quote_buffer
description:
  Extension on StringBuffer providing methods for adding
  strings whose content is delimited by escaped quotation marks
  to the buffer.


version: 0.1.4
homepage: https://github.com/simphotonics/quote_buffer

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:

dev_dependencies:
  ansicolor: ^1.0.2
  benchmark_harness: ^1.0.5
  test: ^1.15.2
