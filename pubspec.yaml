name: quote_buffer
description:
  Extension on StringBuffer that simplifies transforming single objects and lists of objects
  into quoted strings.


version: 0.1.6
homepage: https://github.com/simphotonics/quote_buffer

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:

dev_dependencies:
  ansicolor: ^1.0.2
  benchmark_harness: ^1.0.5
  test: ^1.15.2
